numpy~=1.25.0
tifffile~=2023.4.12
scikit-learn~=1.2.2
tensorflow>=2.12.0

Commands to use while installing necessary libraries:
    pip install tifffile
    pip install tensorflow
    pip install scikit-learn
    pip install numpy

If pip is not in your system, then use the following command to set up pip:
    python -m pip install --upgrade pip